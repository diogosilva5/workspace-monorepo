{
  "name": "workspace-monorepo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "**/expo",
      "**/expo/**"
    ]
  },
  "scripts": {
    "web": "lerna --scope=web run start",
    "mobile": "lerna --scope=mobile run start"
  },
  "devDependencies": {
    "lerna": "^6.0.1"
  }
}
